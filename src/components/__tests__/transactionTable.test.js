// import React from 'react';
// import { shallow } from 'enzyme';
// import TransactionTable from '../TransactionTable';

// describe('TransactionTable Component', () => {

//   it('should render without throwing an error', () => {
//     expect(shallow(<TransactionTable />).find('div.table-container').exists()).toBe(true)
//   });

//   it('renders a date', () => {
//     expect(shallow(<TransactionTable />).find('#date').length).toEqual(1)
//   });

  // it('renders a from', () => {
  //   expect(shallow(<TransactionTable />).find('#from').length).toEqual(1)
  // });

  // it('renders a to', () => {
  //   expect(shallow(<TransactionTable />).find('#to').length).toEqual(1)
  // });

  // it('renders a description', () => {
  //   expect(shallow(<TransactionTable />).find('#description').length).toEqual(1)
  // });

  // it('renders a amount', () => {
  //   expect(shallow(<TransactionTable />).find('#amount').length).toEqual(1)
  // });

  // it('renders a balance', () => {
  //   expect(shallow(<TransactionTable />).find('#balance').length).toEqual(1)
  // });

// });
